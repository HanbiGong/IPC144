#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include "functions.h"


int main(){

    int x = 7, y = 8;
    int res = add (x,y);

    return 0;
}

void display (int rs) {
    printf("RESULT in DISPLAY = %d\n", rs);
}
int add (int x , int y){
    int reslut = (x + y)+SZ;
    display(result);

    retun result;

}